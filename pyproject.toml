[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "calms-experiments"
version = "0.1.0"
description = "CALM-S forecast settlement and budgeted agent orchestration experiments"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
calms = "calms.cli:main"

[tool.setuptools]
packages = ["calms"]
